// Application-wide constants
export const TABLE_CONFIG = {
  DEFAULT_PAGE_SIZE: 3,
  LOADING_DELAY_MS: 800, // Simulated loading delay in milliseconds
  PAGINATION_OPTIONS: [3, 5, 10, 25, 50],
};

// API related constants
export const API_CONFIG = {
  BASE_URL: '/api',
  TIMEOUT_MS: 10000,
  RETRY_COUNT: 3,
};
